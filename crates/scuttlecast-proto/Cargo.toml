[package]
name = "scuttlecast-proto"
description = "Wire protocol and codec for scuttlecast, a reliable multicast transfer tool"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["multicast", "protocol", "codec", "udp"]
categories = ["network-programming", "encoding"]

[dependencies]
bincode.workspace = true
bytes.workspace = true
derive_more = { workspace = true, features = ["display", "deref", "from", "into"] }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
proptest-derive.workspace = true
